import React from 'react';
import NavBar from "./NavBar"

const Header = () => {
    return (
        <header>
            <div id="ext-header">
                <h2>This is a Header!</h2>
            </div>
            <NavBar />
        </header>
    );
}

export default Header;
