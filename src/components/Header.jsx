import React from 'react';
import NavBar from "./NavBar"

const Header = () => {

    return (
        <header>
            <div id="ext-header">
            </div>
            <NavBar />
        </header>
    );
}

export default Header;
